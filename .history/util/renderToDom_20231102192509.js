// renderToDom.js
export function renderCardToDOM(cardElement) {
  const appDiv = document.getElementById("app");
  appDiv.appendChild(cardElement);
  // Modify to fit your specific logic for rendering cards
  // Update card styles and structure as needed
}
