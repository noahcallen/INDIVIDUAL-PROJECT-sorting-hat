// card.js
export function createCard(name, imageUrl, house) {
  const card = document.createElement("div");
  card.classList.add("card");
  // Create and append card HTML based on the provided details
  // Replace with appropriate HTML structure based on your design

  return card;
}
